# Cloudflare Pages parses _redirects line by line. Git's autocrlf on Windows
# would otherwise give it CRLF endings, leaving a trailing \r on the status code
# ("301\r") and silently breaking every rule. Pin it to LF.
_redirects  text eol=lf
